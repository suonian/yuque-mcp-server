# MCP服务器刷频问题解决方案报告

## 1. 问题概述

### 1.1 问题现象
在Claude Code中运行`/mcp list`等MCP命令时，界面出现频繁刷新（刷屏）现象，导致：
- 界面闪烁，影响用户体验
- 复制界面信息时包含大量重复内容
- 命令输出难以阅读和理解

### 1.2 影响范围
- 影响Claude Code中所有MCP命令的使用
- 降低开发效率，影响用户体验
- 导致复制的界面信息包含大量冗余内容

## 2. 问题复现

### 2.1 环境配置
- 操作系统：Windows 10/11
- Claude Code版本：v2.0.76
- MCP服务器：FastMCP 2.14.1
- 项目路径：`D:\AI Trae\server-yuque`

### 2.2 复现步骤
1. 在Claude Code中打开项目
2. 执行`/mcp list`命令
3. 观察界面显示，出现频繁刷新
4. 复制界面内容，包含大量重复信息

## 3. 问题原因分析

### 3.1 主要原因
通过代码分析和测试，确定刷频问题的主要原因：

1. **FastMCP欢迎横幅**：服务器启动时会显示一个大型欢迎横幅，包含ASCII艺术图形和版本信息
2. **过多日志输出**：FastMCP、rich等库产生大量日志信息
3. **重复的界面渲染**：服务器输出导致Claude Code界面频繁重新渲染

### 3.2 技术细节
- FastMCP的`run()`方法默认参数`show_banner=True`，会显示大型欢迎横幅
- 日志系统默认输出到控制台，产生过多信息
- Claude Code对服务器输出敏感，过多输出会导致界面频繁刷新

## 4. 解决方案

### 4.1 核心优化策略
1. 禁用FastMCP欢迎横幅
2. 完全禁用控制台日志输出
3. 移除所有不必要的输出信息
4. 确保只输出MCP协议相关的必要信息

### 4.2 具体实施步骤

#### 4.2.1 禁用FastMCP欢迎横幅
```python
# 在调用mcp.run()时添加show_banner=False参数
mcp.run(show_banner=False)
mcp.run(transport="sse", host=host, port=port, show_banner=False)
```

#### 4.2.2 重新配置日志系统
```python
# 配置logging只输出到文件，不输出到控制台
logging.basicConfig(
    level=logging.CRITICAL,
    filename="yuque_mcp.log",
    filemode="a",
    format="%(asctime)s - %(name)s - %(levelname)s - %(message)s"
)
```

#### 4.2.3 禁用第三方库日志
```python
# 禁用FastMCP、rich等库的日志输出
logging.getLogger("fastmcp").setLevel(logging.CRITICAL)
logging.getLogger("rich").setLevel(logging.CRITICAL)
logging.getLogger("uvicorn").setLevel(logging.CRITICAL)
logging.getLogger("httpx").setLevel(logging.CRITICAL)
```

#### 4.2.4 移除所有控制台handler
```python
# 遍历所有logger，移除控制台handler
for logger_name in logging.root.manager.loggerDict:
    logger = logging.getLogger(logger_name)
    for handler in logger.handlers[:]:
        if isinstance(handler, logging.StreamHandler):
            logger.removeHandler(handler)
```

#### 4.2.5 移除启动日志
```python
# 移除stdio模式下的启动日志输出
# logger.info("启动语雀MCP服务器（stdio模式）")  # 注释或删除此行
mcp.run(show_banner=False)
```

## 5. 优化效果验证

### 5.1 测试方法
1. 启动MCP服务器
2. 在Claude Code中执行`/mcp list`命令
3. 观察界面显示效果
4. 复制界面内容，检查是否包含冗余信息

### 5.2 测试结果
| 测试项 | 测试前 | 测试后 |
|--------|--------|--------|
| 界面刷新 | 频繁刷新，闪烁 | 稳定显示，无闪烁 |
| 输出内容 | 包含欢迎横幅和大量日志 | 只显示必要的MCP信息 |
| 复制内容 | 包含大量重复和冗余信息 | 只包含必要的命令输出 |
| 可读性 | 差，难以阅读 | 好，清晰易读 |

### 5.3 实际效果
- ✅ `mcp list`命令显示稳定，无刷屏现象
- ✅ 界面只显示必要的MCP服务器信息
- ✅ 复制的内容干净，无冗余信息
- ✅ 服务器功能保持完整

## 6. 代码修改记录

### 6.1 修改文件
- 主文件：`D:\AI Trae\server-yuque\yuque_mcp\server.py`

### 6.2 具体修改

| 行号 | 修改前 | 修改后 |
|------|--------|--------|
| 323 | `mcp.run(transport="sse", host=host, port=port)` | `mcp.run(transport="sse", host=host, port=port, show_banner=False)` |
| 327 | `mcp.run()` | `mcp.run(show_banner=False)` |
| 55-74 | 日志配置 | 新增完整的日志优化配置 |
| 343 | 包含启动日志 | 移除启动日志，保持静默 |

## 7. 预防措施

### 7.1 开发阶段
1. 始终使用`show_banner=False`参数启动FastMCP服务器
2. 配置日志只输出到文件，不输出到控制台
3. 设置适当的日志级别，只记录必要的信息
4. 定期检查第三方库的日志配置

### 7.2 部署阶段
1. 确保生产环境中服务器完全静默运行
2. 配置专门的日志文件，便于问题排查
3. 定期清理日志文件，避免占用过多磁盘空间

## 8. 最佳实践

### 8.1 FastMCP使用建议
```python
# 最佳实践：创建并启动FastMCP服务器
mcp = FastMCP(
    name="yuque-mcp-server",
    version="1.0.0",
    instructions="语雀MCP服务器，提供语雀API的MCP接口"
)

# 禁用日志输出到控制台
logging.basicConfig(
    level=logging.CRITICAL,
    filename="mcp_server.log",
    filemode="a"
)

# 禁用第三方库日志
logging.getLogger("fastmcp").setLevel(logging.CRITICAL)
logging.getLogger("rich").setLevel(logging.CRITICAL)

# 启动服务器，禁用欢迎横幅
mcp.run(show_banner=False)
```

### 8.2 Claude Code使用建议
1. 定期更新Claude Code到最新版本
2. 合理配置MCP服务器，避免过多服务器同时运行
3. 使用`/mcp enable`和`/mcp disable`管理MCP服务器

## 9. 结论

### 9.1 问题解决
通过上述优化措施，成功解决了MCP服务器刷频问题，实现了：
- 服务器静默启动，无欢迎横幅
- 日志只输出到文件，不影响界面
- 命令输出清晰，无冗余信息
- 界面稳定，无刷频现象

### 9.2 经验总结
1. 第三方库的默认配置可能不适合生产环境，需要手动优化
2. 控制台输出对交互式界面影响较大，应尽量减少
3. 合理的日志配置对系统维护至关重要
4. 代码优化应兼顾功能完整性和用户体验

## 10. 相关资源

- FastMCP文档：https://gofastmcp.com
- Claude Code MCP文档：https://code.claude.com/docs/en/mcp
- 项目源代码：`D:\AI Trae\server-yuque`

---

**报告编写者**：AI Assistant
**报告日期**：2026-01-02
**版本**：1.0
