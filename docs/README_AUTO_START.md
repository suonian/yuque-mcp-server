# 🚀 自动启动功能快速参考

## 一句话总结

现在您可以通过三种方式自动启动服务，无需每次手动运行 `python3 yuque-proxy.js`。

---

## ⚡ 最快上手（推荐）

### 方式1：使用启动脚本（最简单）

```bash
cd /path/to/yuque-mcpserver
./start_server.sh start    # 启动
./start_server.sh status   # 查看状态
./start_server.sh stop     # 停止
```

### 方式2：安装系统服务（一次配置，永久使用）

```bash
cd /path/to/yuque-mcpserver
./install_service.sh       # 安装服务（只需一次）
# 之后服务会在开机时自动启动，无需手动操作
```

---

## 📁 创建的文件

| 文件 | 功能 | 使用场景 |
|------|------|---------|
| `start_server.sh` | 启动脚本 | 日常手动控制 |
| `install_service.sh` | 服务安装脚本 | 安装系统服务 |
| `com.yuque.mcp.plist` | macOS 服务配置 | 系统服务配置 |
| `auto_start_server.py` | 自动启动包装器 | 脚本集成 |
| `AUTO_START_GUIDE.md` | 详细使用指南 | 完整文档 |

---

## 🎯 使用场景推荐

### 场景1：我每次都要手动启动，很麻烦
**解决方案**：安装系统服务
```bash
./install_service.sh
```
之后服务会在开机时自动启动，无需手动操作。

### 场景2：我只想在使用时启动，用完就停止
**解决方案**：使用启动脚本
```bash
./start_server.sh start   # 使用前启动
./start_server.sh stop    # 使用后停止
```

### 场景3：我想集成到其他脚本中
**解决方案**：使用自动启动包装器
```bash
python3 auto_start_server.py
```

---

## 🔍 常用命令

```bash
# 启动服务
./start_server.sh start

# 查看状态
./start_server.sh status

# 停止服务
./start_server.sh stop

# 重启服务
./start_server.sh restart

# 查看日志
./start_server.sh logs

# 安装系统服务（一次性）
./install_service.sh

# 系统服务管理
launchctl start com.yuque.mcp      # 启动
launchctl stop com.yuque.mcp       # 停止
launchctl list | grep com.yuque.mcp # 状态
```

---

## ✅ 验证服务运行

```bash
# 方法1：使用启动脚本
./start_server.sh status

# 方法2：直接访问健康检查
curl http://localhost:3000/health

# 方法3：在 Chatbox 中测试
# 调用 get_user_info 工具，如果成功说明服务正常
```

---

## 📚 详细文档

- **完整指南**：查看 `AUTO_START_GUIDE.md`
- **配置指南**：查看 `CONFIG_GUIDE.md`
- **测试报告**：查看之前的测试结果

---

## 💡 提示

1. **首次使用**：建议先使用启动脚本测试，确认无误后再安装系统服务
2. **Token 配置**：记得在 Chatbox 的 HTTP Header 中配置 `X-Yuque-Token`
3. **日志位置**：`/tmp/yuque-proxy.log`

---

**现在您再也不用每次手动启动服务了！** 🎉

