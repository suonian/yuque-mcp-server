# 🚀 快速开始指南

## ✅ 配置完成！

您的服务现在已经配置完成并正常运行！

---

## 📋 当前状态

- ✅ 服务已启动（PID: 81990）
- ✅ Token 已配置（从配置文件加载）
- ✅ 健康检查正常
- ✅ 用户: your-username

---

## 🎯 三种配置 Token 的方式

### 方式1：配置文件（推荐，已配置）

```bash
# 查看/编辑配置文件
./start_server.sh config

# 或直接编辑
nano yuque-config.env
```

**优点**：
- ✅ 一次配置，永久使用
- ✅ 启动脚本自动加载
- ✅ 不需要每次手动设置

### 方式2：环境变量

```bash
export YUQUE_TOKEN="your-token-here"
./start_server.sh start
```

### 方式3：HTTP Header（Chatbox 配置）

在 Chatbox 的 MCP Server 配置中，HTTP Header 字段添加：
```
X-Yuque-Token=your-token-here
```

---

## 🔧 常用命令

```bash
# 启动服务
./start_server.sh start

# 查看状态
./start_server.sh status

# 停止服务
./start_server.sh stop

# 重启服务
./start_server.sh restart

# 查看日志
./start_server.sh logs

# 管理配置文件
./start_server.sh config
```

---

## ✅ 验证服务

```bash
# 方法1：使用启动脚本
./start_server.sh status

# 方法2：直接访问健康检查
curl http://localhost:3000/health

# 方法3：在 Chatbox 中测试
# 调用 get_user_info 工具
```

---

## 📝 配置文件说明

配置文件位置：`yuque-config.env`

```bash
# 查看配置
cat yuque-config.env

# 编辑配置
nano yuque-config.env
# 或
vim yuque-config.env
```

**安全提示**：
- ✅ 配置文件已添加到 `.gitignore`，不会被提交到代码仓库
- ✅ 文件权限已设置为 600（仅所有者可读写）

---

## 🔄 下次使用

1. **启动服务**：
   ```bash
   ./start_server.sh start
   ```

2. **配置 Chatbox**：
   - URL: `http://localhost:3000/mcp`
   - HTTP Header: `X-Yuque-Token=your-token`（可选，如果已配置环境变量）

3. **完成！** 现在可以在 Chatbox 中使用语雀 MCP 工具了。

---

## 🎉 完成！

您的语雀 MCP 代理服务已完全配置好，可以正常使用了！

**下一步**：
- 在 Chatbox 中配置 MCP Server
- 开始使用语雀 MCP 工具

---

**提示**：如果遇到问题，查看日志：
```bash
./start_server.sh logs
```

